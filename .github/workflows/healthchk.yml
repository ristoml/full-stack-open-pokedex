name: Health check

on:
    schedule:
      - cron: '5 * * * *'

jobs:
    test_schedule:
      runs-on: ubuntu-latest
      steps:
        - name: Check the deployed service URL
          uses: jtalk/url-health-check-action@v3
          with:          
            url: https://full-stack-open-pokedex-pnh6.onrender.com/health
    